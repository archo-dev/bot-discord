export function CommandsPage() {
  // Implemented in M4 (list + create) and M5 (advanced builder).
  return <p className="text-zinc-400">Le builder de commandes personnalisées arrive à l'étape 4.</p>;
}
