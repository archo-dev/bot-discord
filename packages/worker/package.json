{
  "name": "@bot/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "migrate:local": "wrangler d1 migrations apply botdiscord --local",
    "migrate:remote": "wrangler d1 migrations apply botdiscord --remote"
  },
  "dependencies": {
    "@bot/shared": "workspace:*",
    "discord-api-types": "^0.38.8",
    "hono": "^4.8.4",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20250705.0",
    "typescript": "^5.8.3",
    "vitest": "~3.2.4",
    "wrangler": "^4.23.0"
  }
}
